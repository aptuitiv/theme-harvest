<div class="grid g-4 align-center">
    {% for column in columns %}
        <div class="col-1-1 col-1-2-sm mb-2 mb-0-sm">
            {% for element in column.elements %}
                {{ element.content }}
            {% endfor %}
        </div>
    {% endfor %}
</div>
